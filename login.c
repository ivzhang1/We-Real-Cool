#include<stdio.h>
#include<string.h>
#include <unistd.h>

int main(){
  char
}

int login(){
  char student_name[256];
  char pass[256];
  
  printf("Student Name: ");
  scanf("%s", student_name);
  fflush(stdout);
  
  printf("Password: ");
  scanf("%s", pass);
  fflush(stdout);
}

int registerr(){
  char student_name[256];
  char pass[256];
  
  printf("Student Name: ");
  scanf("%s", student_name);
  fflush(stdout);
  
  printf("Password: ");
  scanf("%s", pass);
  fflush(stdout);

  printf("Confirm Password: ");
  scanf("%s", pass);
  fflush(stdout);  
}
