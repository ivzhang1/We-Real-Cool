#include <stdio.h>
#include "client.h"
#include "commons.h"

int main(int argc, char * argv[]) {
    /*
     * Prompt ip:port (if not given)
     * Connect to socket
     * always:
     *      Prompt for input
     *      Check if loading from file
     *      send to database
     * */
}